<div class="weatherWidget">
    {{#each partials.weatherContext}}
        <div class="location">
            <h3>{{location.name}}</h3>
            <a href="{{location.forecastUrl}}">
                <img src="{{iconUrl}}" alt="{{weather}}">
                {{weather}}, {{temp}}
            </a>
        </div>
    {{/each}}
    <small>Fonte: <a href="https://www.weather.gov/documentation/services-web-api">Serviço de Metereologia</a></small>
</div>